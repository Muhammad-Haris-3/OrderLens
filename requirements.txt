# Pins are the versions the project is actually built and tested against.
#
# They were realigned at M5. The original set was written at M0 and, apart from
# psycopg2 and dotenv, never installed by anything — so CI was resolving one set
# of versions while the work was being done against another. That is not a
# reproducibility claim, it is an untested guess (NFR-1). A stale ruff pin is the
# sharpest edge: a different version reports different lint, so CI and local
# disagree about whether the code passes.

# Warehouse + transformation
dbt-core==1.10.4
dbt-postgres==1.9.0
psycopg2-binary==2.9.12
sqlalchemy==2.0.51
python-dotenv==1.2.2

# Analysis
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
statsmodels==0.14.6
scikit-learn==1.9.0

# Quality
pytest==9.1.1
ruff==0.16.2
pyyaml==6.0.2        # dbt brings it transitively; declared because tests import it directly
